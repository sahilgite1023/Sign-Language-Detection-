# Deployment requirements (lightweight, keypoint-only runtime):
# - Removed TensorFlow; using Mediapipe + TFLite keypoint classifier only.
# - If you re-enable phrase/CNN modes, add tensorflow==2.12.0 back and redeploy.
flask
gunicorn
tensorflow==2.12.0
mediapipe==0.10.11
opencv-python
numpy
matplotlib